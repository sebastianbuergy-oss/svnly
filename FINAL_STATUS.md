# SVNLY Execution Evidence — 21 August 2026

This file is the current execution ledger. It does not declare TestFlight readiness until the signed build and upload have actually completed.

## Build identity

| Item | Value |
|---|---|
| App version | 1.0.0 |
| Build number | 1 locally; Codemagic increments from App Store Connect |
| Latest implementation commit | `4b7bae6` (documentation updates follow) |
| Flutter | 3.47.0 stable, revision `4cf2416426` |
| Dart | 3.13.0 stable |
| iOS deployment target | 16.0 |
| Bundle ID | `ch.sebastianbuergy.svnly` |

## Verified results

- Flutter analysis: **PASS**, final local run reported no issues.
- Flutter tests: **PASS**, 30/30.
- SQL/RLS contract: **PASS**, 28 mandatory tables with RLS, nine critical RPCs and four private buckets.
- Behavioral PostgreSQL RLS matrix: **PASS**, 12/12.
- Release automation tests: **PASS**, 2/2.
- Edge Function type checks: **PASS**, APNs, registration, moderation and scheduled jobs.
- Challenge corpus: **PASS**, 400 bilingual prompts (365 scheduled + 35 reserve).
- Website: **PASS**, production build, 6/6 route tests and public Sites version 2 deployment; privacy, support, terms and community each returned HTTP 200 without a session.
- Codemagic configuration: **PASS**, simulator/native/integration/screenshot workflow and signed TestFlight workflow validate as YAML.
- Codemagic simulator execution: **BLOCKED BEFORE START**, because no SVNLY Git remote/application exists yet.
- Release iOS build/sign/archive: **NOT RUN** (Windows host/no Apple credentials).
- TestFlight: **NOT UPLOADED**.
- App Review: **NOT SUBMITTED**.

## Implemented

Flutter navigation/design, four-screen onboarding, Apple/email auth adapters, age/terms profile setup, server challenge/attempt/retry flow, exact timed camera UI, persisted uploads, locked/private feed, playback/view metrics, reactions/comments/follows/blocks/reports, rankings/streaks/badges backend, settings/support/deletion, RevenueCat adapter/StoreKit config, admin moderation UI, 400 challenge seed, database/RLS/storage migrations, Edge Functions, legal/marketing website and Codemagic workflows. Native iOS now burns selected Live Looks into exported MP4 frames. The moderation path uses trusted server-side FFmpeg frame extraction. APNs delivery, encrypted tokens, notification outbox and lifecycle cron jobs are implemented.

## Genuine release blockers and gaps

1. GitHub repository creation is blocked by a required interactive GitHub sign-in/2FA: a Git transport credential is present, but GitHub REST and GraphQL repository-creation calls returned HTTP 403. Without a remote, Codemagic cannot register or clone SVNLY.
2. The production Supabase/RevenueCat variables and the four encrypted review-account variables are absent. Provisioning is implemented and fail-closed but cannot create the real accounts until those production values exist.
3. Apple App Store Connect signing credentials, Apple app ID and APNs key are absent; no signed IPA or TestFlight upload can truthfully be recorded.
4. The five iOS screenshots are generated by the real simulator app during CI, but their PNG artifacts do not exist until the blocked simulator workflow runs.
5. Physical iPhone camera, microphone, interruption, APNs and StoreKit validation remains after TestFlight processing.
6. Legal text and App Store privacy/age answers remain product drafts pending the controller's final identity/contact and counsel review.

## Deployment

Public website: <https://svnly.sebastian-buergy.chatgpt.site>
Sites project: `appgprj_6a873c0c072c8191a7992b9f75ab6d3e`, version 2.
